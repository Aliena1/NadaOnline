import 'package:flutter/material.dart';

class CommonMarginWidth extends StatelessWidget {
  const CommonMarginWidth({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return SizedBox(width: 20,);
  }
}